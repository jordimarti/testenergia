<div class="row">
  <div class="col-md-12">
    <%= link_to :action => 'index' do %>
      <div class="btn button-page-menu"><span class="glyphicon glyphicon-arrow-left"> </span> Tornar a la llista de projectes</div>
    <% end %>
    <h2 class="page-title">Introducció de dades al formulari ICAEN</h2>
  </div>
</div>
<div class="row">
  <div class="col-md-12">
		<%= form_for(:xproject, :url => {:action => 'write_icaen', :id => @xproject.id, :multipart => true}) do |f| %>

			<div class="form-group"><%= f.label :icaen_xml, "Seleccionar arxiu" %><br />
	    	<%= f.file_field :icaen_xml, :class => 'form-control' %>
	  	</div>

	  	<div><%= f.submit "Pujar arxiu", :class => "btn button-new" %></div>

    <% end %>

	</div>
</div>