<div class="row">
      <div class="col-md-12">
        <h2 class="page-title">Reenviar instruccions de desbloqueig</h2>
     	</div>
    </div>
    <div class="row">

					<%= form_for(resource, :as => resource_name, :url => unlock_path(resource_name), :html => { :method => :post }) do |f| %>
					  <%= devise_error_messages! %>

					  <div><%= f.label :email, "Correu electrònic" %><br />
					  <%= f.email_field :email, :autofocus => true %></div>

					  <div><%= f.submit "Enviar instruccions de desbloqueig" %></div>
					<% end %>

					<%= render "devise/shared/links" %>

			</div>
    </div>
  